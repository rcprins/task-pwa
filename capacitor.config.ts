import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'net.voortman.mytasks',
  appName: 'my-tasks',
  webDir: 'dist/task-pwa/browser'
};

export default config;
